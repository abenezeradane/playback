{
  "name": "playback",
  "version": "0.46.0",
  "description": "A fully featured video player built with Tauri 2.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "svelte-check --tsconfig ./tsconfig.json && vite build",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "//tauri:bundle": "installer-only overlay: libmpv-2.dll + LGPL notices live in tauri.bundle.conf.json (NOT tauri.conf.json) because tauri-build validates resource paths on every cargo build and the DLL is gitignored — listing it in the base config breaks fresh-clone cargo test (native-001 review finding). Run scripts/fetch-libmpv.mjs first.",
    "tauri:bundle": "tauri build --config src-tauri/tauri.bundle.conf.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@tauri-apps/cli": "^2.1.0",
    "svelte": "^5.56.3",
    "svelte-check": "^4.6.0",
    "typescript": "^5.6.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "@tauri-apps/plugin-dialog": "^2.0.1"
  }
}
